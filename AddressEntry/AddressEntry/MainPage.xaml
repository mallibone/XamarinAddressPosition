<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodel="clr-namespace:AddressEntry.ViewModels"
             mc:Ignorable="d"
             xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             x:Class="AddressEntry.MainPage"
             x:DataType="{x:Type viewmodel:MainViewModel}">

    <Grid>
        <StackLayout>
            <Entry x:Name="Street"
                   Placeholder="Street"
                   Text="{Binding Street}" />
            <Entry x:Name="Postalcode"
                   Placeholder="Postalcode"
                   Keyboard="Numeric"
                   Text="{Binding PostalCode}" />
            <Entry x:Name="City"
                   Placeholder="City"
                   Text="{Binding City}" />
            <Entry x:Name="Country"
                   Placeholder="Country"
                   Text="{Binding Country}" />
            <Button Text="Select location on map" Command="{Binding HandleOpenMap}" />
        </StackLayout>
        <maps:Map x:Name="Map" IsVisible="{Binding IsPickingLocationFromMap}" />
    </Grid>

</ContentPage>
